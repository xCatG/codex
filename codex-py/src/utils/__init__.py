# This file makes Python treat the 'utils' directory as a package.
# It can be empty.

# You could put package-level imports here if desired, e.g.:
# from .config import load_config

# Or define __all__ to specify what gets imported with 'from codex_py.src.utils import *'
# __all__ = ["config", "default_providers"]
